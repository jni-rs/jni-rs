# cafebabe API (0.9.0)

A simple parser for Java class files

## 1: Manifest

- Homepage: <https://github.com/staktrace/cafebabe/blob/main/README.md>
- Repository: <https://github.com/staktrace/cafebabe>
- Categories: parsing
- License: 0BSD
- edition: `2018`

### 1.1: Features

- `default`
- `threadsafe`


## 2: README

### cafebabe

![Build Status](https://github.com/staktrace/cafebabe/actions/workflows/test.yml/badge.svg)
[![Crate](https://img.shields.io/crates/v/cafebabe.svg)](https://crates.io/crates/cafebabe)

cafebabe is a parser for `.class` files, which are generated by the javac compiler and other compilers targeting the JVM.
It supports the class file format from Java 21 (September 2023).

There are a bunch of different class file parsers available on crates.io. Main differentiators for `cafebabe` are:

* *Support for Java 21.*
* *A better user API*.
  Most other parsers expose the raw u16 values of the constant pool, which means you as the user end up being responsible for doing the constant pool lookup.
  This also means you have to do type validation (e.g. make sure the constant pool entry you're reading is actually a classinfo type, or whatever).
  `cafebabe` does all this internally and returns the "primitive" type you get after doing the constant pool lookups.
  Consider the example of reading a classinfo member.
  With most other parsers, you get a u16 from the parser, with which you have to:
  (a) do a constant pool lookup,
  (b) verify it's a classinfo type,
  (c) extract the u16 from the classinfo,
  (d) do another constant pool lookup,
  (e) verify it's a utf8 type,
  (f) read the string.
  With `cafebabe` you just get the string right off the bat, since all the type checking and lookups were done during the parsing phase.
* *Zero-copy of string data*.
  All strings are returned as `Cow<'a, str>` which are tied to the lifetime of your input class bytes.
  This means there's zero copying for strings anywhere in `cafebabe` itself, except in the rare case where a string is in the subset of modified "java utf-8" that isn't regular utf-8.
* *Minimal dependencies*.
  The crate only has a couple of dependencies, and neither of those pull in any transitive dependencies.

#### Current status

The main parsing code is fully implemented. All structures (including attributes) described in [Chapter 4 of the JVM spec](https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-4.html) are supported.
The entire `modules` file of the OpenJDK 16/17/21 distributions can be parsed without errors.
`cafebabe` will do some kinds of validation/checking at parse time, but not everything described in Chapter 4 of the JVM spec.
It does all of section 4.8 validation ("Format checking") except possibly some of the attribute validations (the Signature attribute, for instance).
A reasonable goal for this project is to fully implement section 4.8, so patches would be accepted for additional format checking.

#### Q&A

*Why is the project called `cafebabe`?*

Because the first 4 bytes in any valid class file are a magic identifier with the value 0xCAFEBABE. ☕


## 3: Common Traits

The following traits are commonly implemented by types in this crate. Unless otherwise noted, you can assume these traits are implemented:

- `Debug`

- `Freeze`
- `Unpin`

- `any::Any` (`where T: 'static + ?Sized`)
- `borrow::Borrow<T>` (`where T: ?Sized`)
- `borrow::BorrowMut<T>` (`where T: ?Sized`)
- `convert::From<T>`
- `convert::Into<U>` (`where U: convert::From<T>`)
- `convert::TryInto<U>` (`where U: convert::TryFrom<T>`)


## 4: Module: `cafebabe`

### 4.1: Common Traits

In addition to the crate's 'Common Traits', the following traits are commonly implemented by types in this module. Unless otherwise noted, you can assume these traits are implemented:

- `Eq`
- `PartialEq`
- `StructuralPartialEq`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

### 4.2: Structs

#### 4.2.1: `struct cafebabe::AccessFlags`

```rust
pub struct AccessFlags {}
```

_[Private fields hidden]_

##### 4.2.1.1: `impl cafebabe::AccessFlags`

###### 4.2.1.2.1: `const PUBLIC`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.2: `const PRIVATE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.3: `const PROTECTED`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.4: `const STATIC`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.5: `const FINAL`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.6: `const SUPER`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.7: `const SYNCHRONIZED`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.8: `const OPEN`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.9: `const TRANSITIVE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.10: `const VOLATILE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.11: `const BRIDGE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.12: `const STATIC_PHASE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.13: `const TRANSIENT`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.14: `const VARARGS`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.15: `const NATIVE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.16: `const INTERFACE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.17: `const ABSTRACT`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.18: `const STRICT`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.19: `const SYNTHETIC`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.20: `const ANNOTATION`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.21: `const ENUM`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.22: `const MANDATED`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.23: `const MODULE`

_Type: `cafebabe::AccessFlags`_
_Default: `_`_

###### 4.2.1.2.24: `#[inline] fn empty() -> cafebabe::AccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::AccessFlags { ... }
```

Returns an empty set of flags

###### 4.2.1.2.25: `#[inline] fn all() -> cafebabe::AccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::AccessFlags { ... }
```

Returns the set containing all flags.

###### 4.2.1.2.26: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 4.2.1.2.27: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::AccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::AccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 4.2.1.2.28: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::AccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::AccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 4.2.1.2.29: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::AccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::AccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 4.2.1.2.30: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 4.2.1.2.31: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 4.2.1.2.32: `#[inline] fn intersects(self: &Self, other: cafebabe::AccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::AccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 4.2.1.2.33: `#[inline] fn contains(self: &Self, other: cafebabe::AccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::AccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 4.2.1.2.34: `#[inline] fn insert(self: &mut Self, other: cafebabe::AccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::AccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 4.2.1.2.35: `#[inline] fn remove(self: &mut Self, other: cafebabe::AccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::AccessFlags) { ... }
```

Removes the specified flags in-place.

###### 4.2.1.2.36: `#[inline] fn toggle(self: &mut Self, other: cafebabe::AccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::AccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 4.2.1.2.37: `#[inline] fn set(self: &mut Self, other: cafebabe::AccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::AccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 4.2.1.2: Trait Implementations for `AccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialOrd`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::AccessFlags {
        type Output = cafebabe::AccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::AccessFlags {
        type Output = cafebabe::AccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::AccessFlags {
        type Output = cafebabe::AccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::AccessFlags {
        type Output = cafebabe::AccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::AccessFlags {
        type Output = cafebabe::AccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::AccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::AccessFlags>`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 4.2.2: `struct cafebabe::ClassAccessFlags`

```rust
pub struct ClassAccessFlags {}
```

_[Private fields hidden]_

##### 4.2.2.1: `impl cafebabe::ClassAccessFlags`

###### 4.2.2.2.1: `const PUBLIC`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.2: `const FINAL`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.3: `const SUPER`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.4: `const INTERFACE`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.5: `const ABSTRACT`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.6: `const SYNTHETIC`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.7: `const ANNOTATION`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.8: `const ENUM`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.9: `const MODULE`

_Type: `cafebabe::ClassAccessFlags`_
_Default: `_`_

###### 4.2.2.2.10: `#[inline] fn empty() -> cafebabe::ClassAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::ClassAccessFlags { ... }
```

Returns an empty set of flags

###### 4.2.2.2.11: `#[inline] fn all() -> cafebabe::ClassAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::ClassAccessFlags { ... }
```

Returns the set containing all flags.

###### 4.2.2.2.12: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 4.2.2.2.13: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::ClassAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::ClassAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 4.2.2.2.14: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::ClassAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::ClassAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 4.2.2.2.15: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::ClassAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::ClassAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 4.2.2.2.16: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 4.2.2.2.17: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 4.2.2.2.18: `#[inline] fn intersects(self: &Self, other: cafebabe::ClassAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::ClassAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 4.2.2.2.19: `#[inline] fn contains(self: &Self, other: cafebabe::ClassAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::ClassAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 4.2.2.2.20: `#[inline] fn insert(self: &mut Self, other: cafebabe::ClassAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::ClassAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 4.2.2.2.21: `#[inline] fn remove(self: &mut Self, other: cafebabe::ClassAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::ClassAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 4.2.2.2.22: `#[inline] fn toggle(self: &mut Self, other: cafebabe::ClassAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::ClassAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 4.2.2.2.23: `#[inline] fn set(self: &mut Self, other: cafebabe::ClassAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::ClassAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 4.2.2.2: Trait Implementations for `ClassAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialOrd`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::ClassAccessFlags {
        type Output = cafebabe::ClassAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::ClassAccessFlags {
        type Output = cafebabe::ClassAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::ClassAccessFlags {
        type Output = cafebabe::ClassAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::ClassAccessFlags {
        type Output = cafebabe::ClassAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::ClassAccessFlags {
        type Output = cafebabe::ClassAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::ClassAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::ClassAccessFlags>`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 4.2.3: `struct cafebabe::ClassFile<'a>`

```rust
pub struct ClassFile<'a> {
    pub major_version: u16,
    pub minor_version: u16,
    pub access_flags: cafebabe::ClassAccessFlags,
    pub this_class: cafebabe::descriptors::ClassName<'a>,
    pub super_class: option::Option<cafebabe::descriptors::ClassName<'a>>,
    pub interfaces: Vec<cafebabe::descriptors::ClassName<'a>>,
    pub fields: Vec<cafebabe::FieldInfo<'a>>,
    pub methods: Vec<cafebabe::MethodInfo<'a>>,
    pub attributes: Vec<cafebabe::attributes::AttributeInfo<'a>>,
}
```

_[Private fields hidden]_

##### 4.2.3.1: `impl<'a> cafebabe::ClassFile<'a>`

###### 4.2.3.2.1: `#[must_use] fn constantpool_iter(self: &'a Self) -> cafebabe::constant_pool::ConstantPoolIter<'a>`

```rust
#[must_use] pub fn constantpool_iter(self: &'a Self) -> cafebabe::constant_pool::ConstantPoolIter<'a> { ... }
```

##### 4.2.3.2: Trait Implementations for `ClassFile`

**(Note: Does not implement common trait(s): `Eq`, `PartialEq`, `StructuralPartialEq`)**

- `!RefUnwindSafe`
- `!Send`
- `!Sync`
- `!UnwindSafe`

#### 4.2.4: `struct cafebabe::FieldAccessFlags`

```rust
pub struct FieldAccessFlags {}
```

_[Private fields hidden]_

##### 4.2.4.1: `impl cafebabe::FieldAccessFlags`

###### 4.2.4.2.1: `const PUBLIC`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.2: `const PRIVATE`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.3: `const PROTECTED`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.4: `const STATIC`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.5: `const FINAL`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.6: `const VOLATILE`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.7: `const TRANSIENT`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.8: `const SYNTHETIC`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.9: `const ENUM`

_Type: `cafebabe::FieldAccessFlags`_
_Default: `_`_

###### 4.2.4.2.10: `#[inline] fn empty() -> cafebabe::FieldAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::FieldAccessFlags { ... }
```

Returns an empty set of flags

###### 4.2.4.2.11: `#[inline] fn all() -> cafebabe::FieldAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::FieldAccessFlags { ... }
```

Returns the set containing all flags.

###### 4.2.4.2.12: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 4.2.4.2.13: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::FieldAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::FieldAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 4.2.4.2.14: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::FieldAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::FieldAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 4.2.4.2.15: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::FieldAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::FieldAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 4.2.4.2.16: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 4.2.4.2.17: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 4.2.4.2.18: `#[inline] fn intersects(self: &Self, other: cafebabe::FieldAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::FieldAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 4.2.4.2.19: `#[inline] fn contains(self: &Self, other: cafebabe::FieldAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::FieldAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 4.2.4.2.20: `#[inline] fn insert(self: &mut Self, other: cafebabe::FieldAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::FieldAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 4.2.4.2.21: `#[inline] fn remove(self: &mut Self, other: cafebabe::FieldAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::FieldAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 4.2.4.2.22: `#[inline] fn toggle(self: &mut Self, other: cafebabe::FieldAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::FieldAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 4.2.4.2.23: `#[inline] fn set(self: &mut Self, other: cafebabe::FieldAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::FieldAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 4.2.4.2: Trait Implementations for `FieldAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialOrd`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::FieldAccessFlags {
        type Output = cafebabe::FieldAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::FieldAccessFlags {
        type Output = cafebabe::FieldAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::FieldAccessFlags {
        type Output = cafebabe::FieldAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::FieldAccessFlags {
        type Output = cafebabe::FieldAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::FieldAccessFlags {
        type Output = cafebabe::FieldAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::FieldAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::FieldAccessFlags>`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 4.2.5: `struct cafebabe::FieldInfo<'a>`

```rust
pub struct FieldInfo<'a> {
    pub access_flags: cafebabe::FieldAccessFlags,
    pub name: Cow<'a, str>,
    pub descriptor: cafebabe::descriptors::FieldDescriptor<'a>,
    pub attributes: Vec<cafebabe::attributes::AttributeInfo<'a>>,
}
```

##### 4.2.5.1: Trait Implementations for `FieldInfo`

**(Note: Does not implement common trait(s): `Eq`, `PartialEq`, `StructuralPartialEq`)**

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 4.2.6: `struct cafebabe::MethodAccessFlags`

```rust
pub struct MethodAccessFlags {}
```

_[Private fields hidden]_

##### 4.2.6.1: `impl cafebabe::MethodAccessFlags`

###### 4.2.6.2.1: `const PUBLIC`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.2: `const PRIVATE`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.3: `const PROTECTED`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.4: `const STATIC`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.5: `const FINAL`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.6: `const SYNCHRONIZED`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.7: `const BRIDGE`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.8: `const VARARGS`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.9: `const NATIVE`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.10: `const ABSTRACT`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.11: `const STRICT`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.12: `const SYNTHETIC`

_Type: `cafebabe::MethodAccessFlags`_
_Default: `_`_

###### 4.2.6.2.13: `#[inline] fn empty() -> cafebabe::MethodAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::MethodAccessFlags { ... }
```

Returns an empty set of flags

###### 4.2.6.2.14: `#[inline] fn all() -> cafebabe::MethodAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::MethodAccessFlags { ... }
```

Returns the set containing all flags.

###### 4.2.6.2.15: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 4.2.6.2.16: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::MethodAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::MethodAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 4.2.6.2.17: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::MethodAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::MethodAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 4.2.6.2.18: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::MethodAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::MethodAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 4.2.6.2.19: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 4.2.6.2.20: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 4.2.6.2.21: `#[inline] fn intersects(self: &Self, other: cafebabe::MethodAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::MethodAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 4.2.6.2.22: `#[inline] fn contains(self: &Self, other: cafebabe::MethodAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::MethodAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 4.2.6.2.23: `#[inline] fn insert(self: &mut Self, other: cafebabe::MethodAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::MethodAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 4.2.6.2.24: `#[inline] fn remove(self: &mut Self, other: cafebabe::MethodAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::MethodAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 4.2.6.2.25: `#[inline] fn toggle(self: &mut Self, other: cafebabe::MethodAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::MethodAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 4.2.6.2.26: `#[inline] fn set(self: &mut Self, other: cafebabe::MethodAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::MethodAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 4.2.6.2: Trait Implementations for `MethodAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialOrd`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::MethodAccessFlags {
        type Output = cafebabe::MethodAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::MethodAccessFlags {
        type Output = cafebabe::MethodAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::MethodAccessFlags {
        type Output = cafebabe::MethodAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::MethodAccessFlags {
        type Output = cafebabe::MethodAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::MethodAccessFlags {
        type Output = cafebabe::MethodAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::MethodAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::MethodAccessFlags>`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 4.2.7: `struct cafebabe::MethodInfo<'a>`

```rust
pub struct MethodInfo<'a> {
    pub access_flags: cafebabe::MethodAccessFlags,
    pub name: Cow<'a, str>,
    pub descriptor: cafebabe::descriptors::MethodDescriptor<'a>,
    pub attributes: Vec<cafebabe::attributes::AttributeInfo<'a>>,
}
```

##### 4.2.7.1: Trait Implementations for `MethodInfo`

**(Note: Does not implement common trait(s): `Eq`, `PartialEq`, `StructuralPartialEq`)**

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 4.2.8: `struct cafebabe::ParseError`

```rust
pub struct ParseError {}
```

_[Private fields hidden]_

##### 4.2.8.1: Trait Implementations for `ParseError`

- `Display`
- `error::Error`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `ToString` (`where T: Display + ?Sized`)

#### 4.2.9: `struct cafebabe::ParseOptions`

```rust
pub struct ParseOptions {}
```

_[Private fields hidden]_

##### 4.2.9.1: `impl cafebabe::ParseOptions`

###### 4.2.9.2.1: `fn parse_bytecode(self: &mut Self, parse: bool) -> &mut cafebabe::ParseOptions`

Turns on or off parsing of bytecode from the Code attributes of methods. If parsing
is enabled, the [CodeData](crate::attributes::CodeData) structure's optional bytecode
field will be populated (or parsing will fail entirely if bytecode parsing failed).
If parsing is disabled, the [CodeData](crate::attributes::CodeData) structure's optional
bytecode field will be set to None. Parsing is enabled by default, but can be disabled
to speed up parsing in cases where the parsed bytecode is not needed.

##### 4.2.9.2: Trait Implementations for `ParseOptions`

**(Note: Does not implement common trait(s): `Eq`, `PartialEq`, `StructuralPartialEq`)**

- `default::Default`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

### 4.3: Functions

#### 4.3.1: `#[allow(clippy::needless_lifetimes)] fn parse_class<'a>(raw_bytes: &'a [u8]) -> result::Result<cafebabe::ClassFile<'a>, cafebabe::error::ParseError>`

```rust
#[allow(clippy::needless_lifetimes)] pub fn parse_class<'a>(raw_bytes: &'a [u8]) -> result::Result<cafebabe::ClassFile<'a>, cafebabe::error::ParseError> { ... }
```


#### 4.3.2: `fn parse_class_with_options<'a>(raw_bytes: &'a [u8], opts: &cafebabe::ParseOptions) -> result::Result<cafebabe::ClassFile<'a>, cafebabe::error::ParseError>`


## 5: Module: `cafebabe::attributes`

### 5.1: Common Traits

In addition to the crate's 'Common Traits', the following traits are commonly implemented by types in this module. Unless otherwise noted, you can assume these traits are implemented:

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

### 5.2: Structs

#### 5.2.1: `struct cafebabe::attributes::Annotation<'a>`

```rust
pub struct Annotation<'a> {
    pub type_descriptor: cafebabe::descriptors::FieldDescriptor<'a>,
    pub elements: Vec<cafebabe::attributes::AnnotationElement<'a>>,
}
```


#### 5.2.2: `struct cafebabe::attributes::AnnotationElement<'a>`

```rust
pub struct AnnotationElement<'a> {
    pub name: Cow<'a, str>,
    pub value: cafebabe::attributes::AnnotationElementValue<'a>,
}
```


#### 5.2.3: `struct cafebabe::attributes::AttributeInfo<'a>`

```rust
pub struct AttributeInfo<'a> {
    pub name: Cow<'a, str>,
    pub data: cafebabe::attributes::AttributeData<'a>,
}
```


#### 5.2.4: `struct cafebabe::attributes::BootstrapMethodEntry<'a>`

```rust
pub struct BootstrapMethodEntry<'a> {
    pub method: cafebabe::constant_pool::MethodHandle<'a>,
    pub arguments: Vec<cafebabe::constant_pool::BootstrapArgument<'a>>,
}
```


#### 5.2.5: `struct cafebabe::attributes::CodeData<'a>`

```rust
pub struct CodeData<'a> {
    pub max_stack: u16,
    pub max_locals: u16,
    pub code: &'a [u8],
    pub bytecode: option::Option<cafebabe::bytecode::ByteCode<'a>>,
    pub exception_table: Vec<cafebabe::attributes::ExceptionTableEntry<'a>>,
    pub attributes: Vec<cafebabe::attributes::AttributeInfo<'a>>,
}
```


#### 5.2.6: `struct cafebabe::attributes::ExceptionTableEntry<'a>`

```rust
pub struct ExceptionTableEntry<'a> {
    pub start_pc: u16,
    pub end_pc: u16,
    pub handler_pc: u16,
    pub catch_type: option::Option<Cow<'a, str>>,
}
```


#### 5.2.7: `struct cafebabe::attributes::InnerClassAccessFlags`

```rust
pub struct InnerClassAccessFlags {}
```

_[Private fields hidden]_

##### 5.2.7.1: `impl cafebabe::attributes::InnerClassAccessFlags`

###### 5.2.7.2.1: `const PUBLIC`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.2: `const PRIVATE`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.3: `const PROTECTED`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.4: `const STATIC`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.5: `const FINAL`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.6: `const INTERFACE`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.7: `const ABSTRACT`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.8: `const SYNTHETIC`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.9: `const ANNOTATION`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.10: `const ENUM`

_Type: `cafebabe::attributes::InnerClassAccessFlags`_
_Default: `_`_

###### 5.2.7.2.11: `#[inline] fn empty() -> cafebabe::attributes::InnerClassAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::InnerClassAccessFlags { ... }
```

Returns an empty set of flags

###### 5.2.7.2.12: `#[inline] fn all() -> cafebabe::attributes::InnerClassAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::InnerClassAccessFlags { ... }
```

Returns the set containing all flags.

###### 5.2.7.2.13: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.7.2.14: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::InnerClassAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::InnerClassAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.7.2.15: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::InnerClassAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::InnerClassAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.7.2.16: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::InnerClassAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::InnerClassAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.7.2.17: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.7.2.18: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.7.2.19: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::InnerClassAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::InnerClassAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.7.2.20: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::InnerClassAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::InnerClassAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.7.2.21: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.7.2.22: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.7.2.23: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.7.2.24: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::InnerClassAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.7.2: Trait Implementations for `InnerClassAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::InnerClassAccessFlags {
        type Output = cafebabe::attributes::InnerClassAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::InnerClassAccessFlags {
        type Output = cafebabe::attributes::InnerClassAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::InnerClassAccessFlags {
        type Output = cafebabe::attributes::InnerClassAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::InnerClassAccessFlags {
        type Output = cafebabe::attributes::InnerClassAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::InnerClassAccessFlags {
        type Output = cafebabe::attributes::InnerClassAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::InnerClassAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::InnerClassAccessFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.8: `struct cafebabe::attributes::InnerClassEntry<'a>`

```rust
pub struct InnerClassEntry<'a> {
    pub inner_class_info: Cow<'a, str>,
    pub outer_class_info: option::Option<Cow<'a, str>>,
    pub inner_name: option::Option<Cow<'a, str>>,
    pub access_flags: cafebabe::attributes::InnerClassAccessFlags,
}
```


#### 5.2.9: `struct cafebabe::attributes::LineNumberEntry`

```rust
pub struct LineNumberEntry {
    pub start_pc: u16,
    pub line_number: u16,
}
```


#### 5.2.10: `struct cafebabe::attributes::LocalVariableEntry<'a>`

```rust
pub struct LocalVariableEntry<'a> {
    pub start_pc: u16,
    pub length: u16,
    pub name: Cow<'a, str>,
    pub descriptor: cafebabe::descriptors::FieldDescriptor<'a>,
    pub index: u16,
}
```


#### 5.2.11: `struct cafebabe::attributes::LocalVariableTypeEntry<'a>`

```rust
pub struct LocalVariableTypeEntry<'a> {
    pub start_pc: u16,
    pub length: u16,
    pub name: Cow<'a, str>,
    pub signature: Cow<'a, str>,
    pub index: u16,
}
```


#### 5.2.12: `struct cafebabe::attributes::MethodParameterAccessFlags`

```rust
pub struct MethodParameterAccessFlags {}
```

_[Private fields hidden]_

##### 5.2.12.1: `impl cafebabe::attributes::MethodParameterAccessFlags`

###### 5.2.12.2.1: `const FINAL`

_Type: `cafebabe::attributes::MethodParameterAccessFlags`_
_Default: `_`_

###### 5.2.12.2.2: `const SYNTHETIC`

_Type: `cafebabe::attributes::MethodParameterAccessFlags`_
_Default: `_`_

###### 5.2.12.2.3: `const MANDATED`

_Type: `cafebabe::attributes::MethodParameterAccessFlags`_
_Default: `_`_

###### 5.2.12.2.4: `#[inline] fn empty() -> cafebabe::attributes::MethodParameterAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::MethodParameterAccessFlags { ... }
```

Returns an empty set of flags

###### 5.2.12.2.5: `#[inline] fn all() -> cafebabe::attributes::MethodParameterAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::MethodParameterAccessFlags { ... }
```

Returns the set containing all flags.

###### 5.2.12.2.6: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.12.2.7: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::MethodParameterAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::MethodParameterAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.12.2.8: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::MethodParameterAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::MethodParameterAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.12.2.9: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::MethodParameterAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::MethodParameterAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.12.2.10: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.12.2.11: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.12.2.12: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::MethodParameterAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::MethodParameterAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.12.2.13: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::MethodParameterAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::MethodParameterAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.12.2.14: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.12.2.15: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.12.2.16: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.12.2.17: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::MethodParameterAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.12.2: Trait Implementations for `MethodParameterAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::MethodParameterAccessFlags {
        type Output = cafebabe::attributes::MethodParameterAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::MethodParameterAccessFlags {
        type Output = cafebabe::attributes::MethodParameterAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::MethodParameterAccessFlags {
        type Output = cafebabe::attributes::MethodParameterAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::MethodParameterAccessFlags {
        type Output = cafebabe::attributes::MethodParameterAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::MethodParameterAccessFlags {
        type Output = cafebabe::attributes::MethodParameterAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::MethodParameterAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::MethodParameterAccessFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.13: `struct cafebabe::attributes::MethodParameterEntry<'a>`

```rust
pub struct MethodParameterEntry<'a> {
    pub name: option::Option<Cow<'a, str>>,
    pub access_flags: cafebabe::attributes::MethodParameterAccessFlags,
}
```


#### 5.2.14: `struct cafebabe::attributes::ModuleAccessFlags`

```rust
pub struct ModuleAccessFlags {}
```

_[Private fields hidden]_

##### 5.2.14.1: `impl cafebabe::attributes::ModuleAccessFlags`

###### 5.2.14.2.1: `const OPEN`

_Type: `cafebabe::attributes::ModuleAccessFlags`_
_Default: `_`_

###### 5.2.14.2.2: `const SYNTHETIC`

_Type: `cafebabe::attributes::ModuleAccessFlags`_
_Default: `_`_

###### 5.2.14.2.3: `const MANDATED`

_Type: `cafebabe::attributes::ModuleAccessFlags`_
_Default: `_`_

###### 5.2.14.2.4: `#[inline] fn empty() -> cafebabe::attributes::ModuleAccessFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::ModuleAccessFlags { ... }
```

Returns an empty set of flags

###### 5.2.14.2.5: `#[inline] fn all() -> cafebabe::attributes::ModuleAccessFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::ModuleAccessFlags { ... }
```

Returns the set containing all flags.

###### 5.2.14.2.6: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.14.2.7: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleAccessFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleAccessFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.14.2.8: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleAccessFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleAccessFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.14.2.9: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleAccessFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleAccessFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.14.2.10: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.14.2.11: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.14.2.12: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::ModuleAccessFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::ModuleAccessFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.14.2.13: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::ModuleAccessFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::ModuleAccessFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.14.2.14: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.14.2.15: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.14.2.16: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.14.2.17: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::ModuleAccessFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.14.2: Trait Implementations for `ModuleAccessFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::ModuleAccessFlags {
        type Output = cafebabe::attributes::ModuleAccessFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::ModuleAccessFlags {
        type Output = cafebabe::attributes::ModuleAccessFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::ModuleAccessFlags {
        type Output = cafebabe::attributes::ModuleAccessFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::ModuleAccessFlags {
        type Output = cafebabe::attributes::ModuleAccessFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::ModuleAccessFlags {
        type Output = cafebabe::attributes::ModuleAccessFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::ModuleAccessFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::ModuleAccessFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.15: `struct cafebabe::attributes::ModuleData<'a>`

```rust
pub struct ModuleData<'a> {
    pub name: Cow<'a, str>,
    pub access_flags: cafebabe::attributes::ModuleAccessFlags,
    pub version: option::Option<Cow<'a, str>>,
    pub requires: Vec<cafebabe::attributes::ModuleRequireEntry<'a>>,
    pub exports: Vec<cafebabe::attributes::ModuleExportsEntry<'a>>,
    pub opens: Vec<cafebabe::attributes::ModuleOpensEntry<'a>>,
    pub uses: Vec<Cow<'a, str>>,
    pub provides: Vec<cafebabe::attributes::ModuleProvidesEntry<'a>>,
}
```


#### 5.2.16: `struct cafebabe::attributes::ModuleExportsEntry<'a>`

```rust
pub struct ModuleExportsEntry<'a> {
    pub package_name: Cow<'a, str>,
    pub flags: cafebabe::attributes::ModuleExportsFlags,
    pub exports_to: Vec<Cow<'a, str>>,
}
```


#### 5.2.17: `struct cafebabe::attributes::ModuleExportsFlags`

```rust
pub struct ModuleExportsFlags {}
```

_[Private fields hidden]_

##### 5.2.17.1: `impl cafebabe::attributes::ModuleExportsFlags`

###### 5.2.17.2.1: `const SYNTHETIC`

_Type: `cafebabe::attributes::ModuleExportsFlags`_
_Default: `_`_

###### 5.2.17.2.2: `const MANDATED`

_Type: `cafebabe::attributes::ModuleExportsFlags`_
_Default: `_`_

###### 5.2.17.2.3: `#[inline] fn empty() -> cafebabe::attributes::ModuleExportsFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::ModuleExportsFlags { ... }
```

Returns an empty set of flags

###### 5.2.17.2.4: `#[inline] fn all() -> cafebabe::attributes::ModuleExportsFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::ModuleExportsFlags { ... }
```

Returns the set containing all flags.

###### 5.2.17.2.5: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.17.2.6: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleExportsFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleExportsFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.17.2.7: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleExportsFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleExportsFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.17.2.8: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleExportsFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleExportsFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.17.2.9: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.17.2.10: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.17.2.11: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::ModuleExportsFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::ModuleExportsFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.17.2.12: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::ModuleExportsFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::ModuleExportsFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.17.2.13: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.17.2.14: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.17.2.15: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.17.2.16: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::ModuleExportsFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.17.2: Trait Implementations for `ModuleExportsFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::ModuleExportsFlags {
        type Output = cafebabe::attributes::ModuleExportsFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::ModuleExportsFlags {
        type Output = cafebabe::attributes::ModuleExportsFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::ModuleExportsFlags {
        type Output = cafebabe::attributes::ModuleExportsFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::ModuleExportsFlags {
        type Output = cafebabe::attributes::ModuleExportsFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::ModuleExportsFlags {
        type Output = cafebabe::attributes::ModuleExportsFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::ModuleExportsFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::ModuleExportsFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.18: `struct cafebabe::attributes::ModuleOpensEntry<'a>`

```rust
pub struct ModuleOpensEntry<'a> {
    pub package_name: Cow<'a, str>,
    pub flags: cafebabe::attributes::ModuleOpensFlags,
    pub opens_to: Vec<Cow<'a, str>>,
}
```


#### 5.2.19: `struct cafebabe::attributes::ModuleOpensFlags`

```rust
pub struct ModuleOpensFlags {}
```

_[Private fields hidden]_

##### 5.2.19.1: `impl cafebabe::attributes::ModuleOpensFlags`

###### 5.2.19.2.1: `const SYNTHETIC`

_Type: `cafebabe::attributes::ModuleOpensFlags`_
_Default: `_`_

###### 5.2.19.2.2: `const MANDATED`

_Type: `cafebabe::attributes::ModuleOpensFlags`_
_Default: `_`_

###### 5.2.19.2.3: `#[inline] fn empty() -> cafebabe::attributes::ModuleOpensFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::ModuleOpensFlags { ... }
```

Returns an empty set of flags

###### 5.2.19.2.4: `#[inline] fn all() -> cafebabe::attributes::ModuleOpensFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::ModuleOpensFlags { ... }
```

Returns the set containing all flags.

###### 5.2.19.2.5: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.19.2.6: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleOpensFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleOpensFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.19.2.7: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleOpensFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleOpensFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.19.2.8: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleOpensFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleOpensFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.19.2.9: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.19.2.10: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.19.2.11: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::ModuleOpensFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::ModuleOpensFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.19.2.12: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::ModuleOpensFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::ModuleOpensFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.19.2.13: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.19.2.14: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.19.2.15: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.19.2.16: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::ModuleOpensFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.19.2: Trait Implementations for `ModuleOpensFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::ModuleOpensFlags {
        type Output = cafebabe::attributes::ModuleOpensFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::ModuleOpensFlags {
        type Output = cafebabe::attributes::ModuleOpensFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::ModuleOpensFlags {
        type Output = cafebabe::attributes::ModuleOpensFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::ModuleOpensFlags {
        type Output = cafebabe::attributes::ModuleOpensFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::ModuleOpensFlags {
        type Output = cafebabe::attributes::ModuleOpensFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::ModuleOpensFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::ModuleOpensFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.20: `struct cafebabe::attributes::ModuleProvidesEntry<'a>`

```rust
pub struct ModuleProvidesEntry<'a> {
    pub service_interface_name: Cow<'a, str>,
    pub provides_with: Vec<Cow<'a, str>>,
}
```


#### 5.2.21: `struct cafebabe::attributes::ModuleRequireEntry<'a>`

```rust
pub struct ModuleRequireEntry<'a> {
    pub name: Cow<'a, str>,
    pub flags: cafebabe::attributes::ModuleRequiresFlags,
    pub version: option::Option<Cow<'a, str>>,
}
```


#### 5.2.22: `struct cafebabe::attributes::ModuleRequiresFlags`

```rust
pub struct ModuleRequiresFlags {}
```

_[Private fields hidden]_

##### 5.2.22.1: `impl cafebabe::attributes::ModuleRequiresFlags`

###### 5.2.22.2.1: `const TRANSITIVE`

_Type: `cafebabe::attributes::ModuleRequiresFlags`_
_Default: `_`_

###### 5.2.22.2.2: `const STATIC_PHASE`

_Type: `cafebabe::attributes::ModuleRequiresFlags`_
_Default: `_`_

###### 5.2.22.2.3: `const SYNTHETIC`

_Type: `cafebabe::attributes::ModuleRequiresFlags`_
_Default: `_`_

###### 5.2.22.2.4: `const MANDATED`

_Type: `cafebabe::attributes::ModuleRequiresFlags`_
_Default: `_`_

###### 5.2.22.2.5: `#[inline] fn empty() -> cafebabe::attributes::ModuleRequiresFlags`

```rust
#[inline] pub const fn empty() -> cafebabe::attributes::ModuleRequiresFlags { ... }
```

Returns an empty set of flags

###### 5.2.22.2.6: `#[inline] fn all() -> cafebabe::attributes::ModuleRequiresFlags`

```rust
#[inline] pub const fn all() -> cafebabe::attributes::ModuleRequiresFlags { ... }
```

Returns the set containing all flags.

###### 5.2.22.2.7: `#[inline] fn bits(self: &Self) -> u16`

```rust
#[inline] pub const fn bits(self: &Self) -> u16 { ... }
```

Returns the raw value of the flags currently stored.

###### 5.2.22.2.8: `#[inline] fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleRequiresFlags>`

```rust
#[inline] pub fn from_bits(bits: u16) -> option::Option<cafebabe::attributes::ModuleRequiresFlags> { ... }
```

Convert from underlying bit representation, unless that
representation contains bits that do not correspond to a flag.

###### 5.2.22.2.9: `#[inline] fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleRequiresFlags`

```rust
#[inline] pub const fn from_bits_truncate(bits: u16) -> cafebabe::attributes::ModuleRequiresFlags { ... }
```

Convert from underlying bit representation, dropping any bits
that do not correspond to flags.

###### 5.2.22.2.10: `#[inline] fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleRequiresFlags`

```rust
#[inline] pub const unsafe fn from_bits_unchecked(bits: u16) -> cafebabe::attributes::ModuleRequiresFlags { ... }
```

Convert from underlying bit representation, preserving all
bits (even those not corresponding to a defined flag).

###### 5.2.22.2.11: `#[inline] fn is_empty(self: &Self) -> bool`

```rust
#[inline] pub const fn is_empty(self: &Self) -> bool { ... }
```

Returns `true` if no flags are currently stored.

###### 5.2.22.2.12: `#[inline] fn is_all(self: &Self) -> bool`

```rust
#[inline] pub const fn is_all(self: &Self) -> bool { ... }
```

Returns `true` if all flags are currently set.

###### 5.2.22.2.13: `#[inline] fn intersects(self: &Self, other: cafebabe::attributes::ModuleRequiresFlags) -> bool`

```rust
#[inline] pub const fn intersects(self: &Self, other: cafebabe::attributes::ModuleRequiresFlags) -> bool { ... }
```

Returns `true` if there are flags common to both `self` and `other`.

###### 5.2.22.2.14: `#[inline] fn contains(self: &Self, other: cafebabe::attributes::ModuleRequiresFlags) -> bool`

```rust
#[inline] pub const fn contains(self: &Self, other: cafebabe::attributes::ModuleRequiresFlags) -> bool { ... }
```

Returns `true` all of the flags in `other` are contained within `self`.

###### 5.2.22.2.15: `#[inline] fn insert(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags)`

```rust
#[inline] pub fn insert(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags) { ... }
```

Inserts the specified flags in-place.

###### 5.2.22.2.16: `#[inline] fn remove(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags)`

```rust
#[inline] pub fn remove(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags) { ... }
```

Removes the specified flags in-place.

###### 5.2.22.2.17: `#[inline] fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags)`

```rust
#[inline] pub fn toggle(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags) { ... }
```

Toggles the specified flags in-place.

###### 5.2.22.2.18: `#[inline] fn set(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags, value: bool)`

```rust
#[inline] pub fn set(self: &mut Self, other: cafebabe::attributes::ModuleRequiresFlags, value: bool) { ... }
```

Inserts or removes the specified flags depending on the passed value.

##### 5.2.22.2: Trait Implementations for `ModuleRequiresFlags`

- `Binary`
- `Clone`
- `Copy`
- `Eq`
- `Hash`
- `LowerHex`
- `Octal`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`
- `UpperHex`
- `arith::SubAssign`
- `bit::BitAndAssign`
- `bit::BitOrAssign`
- `bit::BitXorAssign`

- `arith::Sub`

    ```rust
    impl arith::Sub for cafebabe::attributes::ModuleRequiresFlags {
        type Output = cafebabe::attributes::ModuleRequiresFlags;
    }
    ```
- `bit::BitAnd`

    ```rust
    impl bit::BitAnd for cafebabe::attributes::ModuleRequiresFlags {
        type Output = cafebabe::attributes::ModuleRequiresFlags;
    }
    ```
- `bit::BitOr`

    ```rust
    impl bit::BitOr for cafebabe::attributes::ModuleRequiresFlags {
        type Output = cafebabe::attributes::ModuleRequiresFlags;
    }
    ```
- `bit::BitXor`

    ```rust
    impl bit::BitXor for cafebabe::attributes::ModuleRequiresFlags {
        type Output = cafebabe::attributes::ModuleRequiresFlags;
    }
    ```
- `bit::Not`

    ```rust
    impl bit::Not for cafebabe::attributes::ModuleRequiresFlags {
        type Output = cafebabe::attributes::ModuleRequiresFlags;
    }
    ```
- `iter::traits::collect::Extend<cafebabe::attributes::ModuleRequiresFlags>`
- `iter::traits::collect::FromIterator<cafebabe::attributes::ModuleRequiresFlags>`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)

#### 5.2.23: `struct cafebabe::attributes::ParameterAnnotation<'a>`

```rust
pub struct ParameterAnnotation<'a> {
    pub annotations: Vec<cafebabe::attributes::Annotation<'a>>,
}
```


#### 5.2.24: `struct cafebabe::attributes::RecordComponentEntry<'a>`

```rust
pub struct RecordComponentEntry<'a> {
    pub name: Cow<'a, str>,
    pub descriptor: cafebabe::descriptors::FieldDescriptor<'a>,
    pub attributes: Vec<cafebabe::attributes::AttributeInfo<'a>>,
}
```


#### 5.2.25: `struct cafebabe::attributes::TypeAnnotation<'a>`

```rust
pub struct TypeAnnotation<'a> {
    pub target_type: cafebabe::attributes::TypeAnnotationTarget,
    pub target_path: Vec<cafebabe::attributes::TypeAnnotationTargetPathEntry>,
    pub annotation: cafebabe::attributes::Annotation<'a>,
}
```


#### 5.2.26: `struct cafebabe::attributes::TypeAnnotationLocalVarTargetEntry`

```rust
pub struct TypeAnnotationLocalVarTargetEntry {
    pub start_pc: u16,
    pub length: u16,
    pub index: u16,
}
```


#### 5.2.27: `struct cafebabe::attributes::TypeAnnotationTargetPathEntry`

```rust
pub struct TypeAnnotationTargetPathEntry {
    pub path_kind: cafebabe::attributes::TypeAnnotationTargetPathKind,
    pub argument_index: u8,
}
```


### 5.3: Enums

#### 5.3.1: `enum cafebabe::attributes::AnnotationElementValue<'a>`

```rust
pub enum AnnotationElementValue<'a> {
    ByteConstant(i32),
    CharConstant(i32),
    DoubleConstant(f64),
    FloatConstant(f32),
    IntConstant(i32),
    LongConstant(i64),
    ShortConstant(i32),
    BooleanConstant(i32),
    StringConstant(Cow<'a, str>),
    EnumConstant{ type_name: cafebabe::descriptors::FieldDescriptor<'a>, const_name: Cow<'a, str> },
    ClassLiteral{ class_name: Cow<'a, str> },
    AnnotationValue(cafebabe::attributes::Annotation<'a>),
    ArrayValue(Vec<cafebabe::attributes::AnnotationElementValue<'a>>),
}
```


#### 5.3.2: `enum cafebabe::attributes::AttributeData<'a>`

```rust
pub enum AttributeData<'a> {
    ConstantValue(cafebabe::constant_pool::LiteralConstant<'a>),
    Code(cafebabe::attributes::CodeData<'a>),
    StackMapTable(Vec<cafebabe::attributes::StackMapEntry<'a>>),
    Exceptions(Vec<Cow<'a, str>>),
    InnerClasses(Vec<cafebabe::attributes::InnerClassEntry<'a>>),
    EnclosingMethod{ class_name: Cow<'a, str>, method: option::Option<cafebabe::constant_pool::NameAndType<'a>> },
    Synthetic,
    Signature(Cow<'a, str>),
    SourceFile(Cow<'a, str>),
    SourceDebugExtension(Cow<'a, str>),
    LineNumberTable(Vec<cafebabe::attributes::LineNumberEntry>),
    LocalVariableTable(Vec<cafebabe::attributes::LocalVariableEntry<'a>>),
    LocalVariableTypeTable(Vec<cafebabe::attributes::LocalVariableTypeEntry<'a>>),
    Deprecated,
    RuntimeVisibleAnnotations(Vec<cafebabe::attributes::Annotation<'a>>),
    RuntimeInvisibleAnnotations(Vec<cafebabe::attributes::Annotation<'a>>),
    RuntimeVisibleParameterAnnotations(Vec<cafebabe::attributes::ParameterAnnotation<'a>>),
    RuntimeInvisibleParameterAnnotations(Vec<cafebabe::attributes::ParameterAnnotation<'a>>),
    RuntimeVisibleTypeAnnotations(Vec<cafebabe::attributes::TypeAnnotation<'a>>),
    RuntimeInvisibleTypeAnnotations(Vec<cafebabe::attributes::TypeAnnotation<'a>>),
    AnnotationDefault(cafebabe::attributes::AnnotationElementValue<'a>),
    BootstrapMethods(Vec<cafebabe::attributes::BootstrapMethodEntry<'a>>),
    MethodParameters(Vec<cafebabe::attributes::MethodParameterEntry<'a>>),
    Module(cafebabe::attributes::ModuleData<'a>),
    ModulePackages(Vec<Cow<'a, str>>),
    ModuleMainClass(Cow<'a, str>),
    NestHost(Cow<'a, str>),
    NestMembers(Vec<Cow<'a, str>>),
    PermittedSubclasses(Vec<Cow<'a, str>>),
    Record(Vec<cafebabe::attributes::RecordComponentEntry<'a>>),
    Other(&'a [u8]),
}
```


#### 5.3.3: `enum cafebabe::attributes::StackMapEntry<'a>`

```rust
pub enum StackMapEntry<'a> {
    Same{ offset_delta: u16 },
    SameLocals1StackItem{ offset_delta: u16, stack: cafebabe::attributes::VerificationType<'a> },
    Chop{ offset_delta: u16, chop_count: u16 },
    Append{ offset_delta: u16, locals: Vec<cafebabe::attributes::VerificationType<'a>> },
    FullFrame{ offset_delta: u16, locals: Vec<cafebabe::attributes::VerificationType<'a>>, stack: Vec<cafebabe::attributes::VerificationType<'a>> },
}
```


#### 5.3.4: `enum cafebabe::attributes::TypeAnnotationTarget`

```rust
pub enum TypeAnnotationTarget {
    TypeParameter{ index: u8 },
    Supertype{ index: u16 },
    TypeParameterBound{ type_parameter_index: u8, bound_index: u8 },
    Empty,
    FormalParameter{ index: u8 },
    Throws{ index: u16 },
    LocalVar(Vec<cafebabe::attributes::TypeAnnotationLocalVarTargetEntry>),
    Catch{ exception_table_index: u16 },
    Offset{ offset: u16 },
    TypeArgument{ offset: u16, type_argument_index: u8 },
}
```


#### 5.3.5: `enum cafebabe::attributes::TypeAnnotationTargetPathKind`

```rust
pub enum TypeAnnotationTargetPathKind {
    DeeperArray,
    DeeperNested,
    WildcardTypeArgument,
    TypeArgument,
}
```


#### 5.3.6: `enum cafebabe::attributes::VerificationType<'a>`

```rust
pub enum VerificationType<'a> {
    Top,
    Integer,
    Float,
    Long,
    Double,
    Null,
    UninitializedThis,
    Uninitialized{ code_offset: u16 },
    Object{ class_name: Cow<'a, str> },
}
```


## 6: Module: `cafebabe::bytecode`

### 6.1: Common Traits

In addition to the crate's 'Common Traits', the following traits are commonly implemented by types in this module. Unless otherwise noted, you can assume these traits are implemented:

- `Clone`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)
- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

### 6.2: Structs

#### 6.2.1: `struct cafebabe::bytecode::ByteCode<'a>`

```rust
pub struct ByteCode<'a> {
    pub opcodes: Vec<(usize, cafebabe::bytecode::Opcode<'a>)>,
}
```

##### 6.2.1.1: Fields

###### 6.2.1.1.1: `opcodes`

This contains pairs of (offset, opcode) where offset is the offset of the start
of the opcode in bytes from the beginning of the data section of the Code attribute.
This array will always be sorted in increasing offset order. The `get_opcode_index`
function can be used to look up the vector index for a particular offset.

##### 6.2.1.2: `impl<'a> cafebabe::bytecode::ByteCode<'a>`

###### 6.2.1.3.1: `fn get_opcode_index(self: &Self, offset: usize) -> option::Option<usize>`

Given an offset (in bytes) into the bytecode array, return the index into
`self.opcodes` of the corresponding opcode. If there is no corresponding
opcode at that offset, returns None.

##### 6.2.1.3: Trait Implementations for `ByteCode`

**(Note: Does not implement common trait(s): `Clone`, `CloneToUninit`, `ToOwned`)**


#### 6.2.2: `struct cafebabe::bytecode::LookupTable`

```rust
pub struct LookupTable {
    pub default: cafebabe::bytecode::JumpOffset,
    pub match_offsets: Vec<(i32, cafebabe::bytecode::JumpOffset)>,
}
```


#### 6.2.3: `struct cafebabe::bytecode::RangeTable`

```rust
pub struct RangeTable {
    pub default: cafebabe::bytecode::JumpOffset,
    pub low: i32,
    pub high: i32,
    pub jumps: Vec<cafebabe::bytecode::JumpOffset>,
}
```


### 6.3: Enums

#### 6.3.1: `enum cafebabe::bytecode::Opcode<'a>`

```rust
pub enum Opcode<'a> {
    Aaload,
    Aastore,
    AconstNull,
    Aload(u16),
    Anewarray(cafebabe::constant_pool::ObjectArrayType<'a>),
    Areturn,
    Arraylength,
    Astore(u16),
    Athrow,
    Baload,
    Bastore,
    Bipush(i8),
    Breakpoint,
    Caload,
    Castore,
    Checkcast(cafebabe::constant_pool::ObjectArrayType<'a>),
    D2f,
    D2i,
    D2l,
    Dadd,
    Daload,
    Dastore,
    Dcmpg,
    Dcmpl,
    Dconst0,
    Dconst1,
    Ddiv,
    Dload(u16),
    Dmul,
    Dneg,
    Drem,
    Dreturn,
    Dstore(u16),
    Dsub,
    Dup,
    DupX1,
    DupX2,
    Dup2,
    Dup2X1,
    Dup2X2,
    F2d,
    F2i,
    F2l,
    Fadd,
    Faload,
    Fastore,
    Fcmpg,
    Fcmpl,
    Fconst0,
    Fconst1,
    Fconst2,
    Fdiv,
    Fload(u16),
    Fmul,
    Fneg,
    Frem,
    Freturn,
    Fstore(u16),
    Fsub,
    Getfield(cafebabe::constant_pool::MemberRef<'a>),
    Getstatic(cafebabe::constant_pool::MemberRef<'a>),
    Goto(cafebabe::bytecode::JumpOffset),
    I2b,
    I2c,
    I2d,
    I2f,
    I2l,
    I2s,
    Iadd,
    Iaload,
    Iand,
    Iastore,
    IconstM1,
    Iconst0,
    Iconst1,
    Iconst2,
    Iconst3,
    Iconst4,
    Iconst5,
    Idiv,
    IfAcmpeq(cafebabe::bytecode::JumpOffset),
    IfAcmpne(cafebabe::bytecode::JumpOffset),
    IfIcmpeq(cafebabe::bytecode::JumpOffset),
    IfIcmpge(cafebabe::bytecode::JumpOffset),
    IfIcmpgt(cafebabe::bytecode::JumpOffset),
    IfIcmple(cafebabe::bytecode::JumpOffset),
    IfIcmplt(cafebabe::bytecode::JumpOffset),
    IfIcmpne(cafebabe::bytecode::JumpOffset),
    Ifeq(cafebabe::bytecode::JumpOffset),
    Ifge(cafebabe::bytecode::JumpOffset),
    Ifgt(cafebabe::bytecode::JumpOffset),
    Ifle(cafebabe::bytecode::JumpOffset),
    Iflt(cafebabe::bytecode::JumpOffset),
    Ifne(cafebabe::bytecode::JumpOffset),
    Ifnonnull(cafebabe::bytecode::JumpOffset),
    Ifnull(cafebabe::bytecode::JumpOffset),
    Iinc(u16, i16),
    Iload(u16),
    Impdep1,
    Impdep2,
    Imul,
    Ineg,
    Instanceof(cafebabe::constant_pool::ObjectArrayType<'a>),
    Invokedynamic(cafebabe::constant_pool::InvokeDynamic<'a>),
    Invokeinterface(cafebabe::constant_pool::MemberRef<'a>, u8),
    Invokespecial(cafebabe::constant_pool::MemberRef<'a>),
    Invokestatic(cafebabe::constant_pool::MemberRef<'a>),
    Invokevirtual(cafebabe::constant_pool::MemberRef<'a>),
    Ior,
    Irem,
    Ireturn,
    Ishl,
    Ishr,
    Istore(u16),
    Isub,
    Iushr,
    Ixor,
    Jsr(cafebabe::bytecode::JumpOffset),
    L2d,
    L2f,
    L2i,
    Ladd,
    Laload,
    Land,
    Lastore,
    Lcmp,
    Lconst0,
    Lconst1,
    Ldc(cafebabe::constant_pool::Loadable<'a>),
    LdcW(cafebabe::constant_pool::Loadable<'a>),
    Ldc2W(cafebabe::constant_pool::Loadable<'a>),
    Ldiv,
    Lload(u16),
    Lmul,
    Lneg,
    Lookupswitch(cafebabe::bytecode::LookupTable),
    Lor,
    Lrem,
    Lreturn,
    Lshl,
    Lshr,
    Lstore(u16),
    Lsub,
    Lushr,
    Lxor,
    Monitorenter,
    Monitorexit,
    Multianewarray(cafebabe::constant_pool::ObjectArrayType<'a>, u8),
    New(Cow<'a, str>),
    Newarray(cafebabe::bytecode::PrimitiveArrayType),
    Nop,
    Pop,
    Pop2,
    Putfield(cafebabe::constant_pool::MemberRef<'a>),
    Putstatic(cafebabe::constant_pool::MemberRef<'a>),
    Ret(u16),
    Return,
    Saload,
    Sastore,
    Sipush(i16),
    Swap,
    Tableswitch(cafebabe::bytecode::RangeTable),
}
```


#### 6.3.2: `enum cafebabe::bytecode::PrimitiveArrayType`

```rust
pub enum PrimitiveArrayType {
    Boolean,
    Char,
    Float,
    Double,
    Byte,
    Short,
    Int,
    Long,
}
```

##### 6.3.2.1: Trait Implementations for `PrimitiveArrayType`

- `Copy`
- `Eq`
- `Hash`
- `Ord`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`

### 6.4: Type Aliases

#### 6.4.1: `type JumpOffset`


## 7: Module: `cafebabe::constant_pool`

### 7.1: Common Traits

In addition to the crate's 'Common Traits', the following traits are commonly implemented by types in this module. Unless otherwise noted, you can assume these traits are implemented:

- `Clone`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)
- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

### 7.2: Structs

#### 7.2.1: `struct cafebabe::constant_pool::ConstantPoolIter<'a>`

```rust
pub struct ConstantPoolIter<'a> {}
```

_[Private fields hidden]_

##### 7.2.1.1: Trait Implementations for `ConstantPoolIter`

**(Note: Does not implement common trait(s): `Clone`, `CloneToUninit`, `Debug`, `ToOwned`)**

- `iter::traits::iterator::Iterator`

    ```rust
    impl<'a> iter::traits::iterator::Iterator for cafebabe::constant_pool::ConstantPoolIter<'a> {
        type Item = cafebabe::constant_pool::ConstantPoolItem<'a>;
    }
    ```

- `!RefUnwindSafe`
- `!Send`
- `!Sync`
- `!UnwindSafe`

- `iter::traits::collect::IntoIterator` (`where I: iter::traits::iterator::Iterator`)

#### 7.2.2: `struct cafebabe::constant_pool::Dynamic<'a>`

```rust
pub struct Dynamic<'a> {
    pub attr_index: u16,
    pub name_and_type: cafebabe::constant_pool::NameAndType<'a>,
}
```

##### 7.2.2.1: Trait Implementations for `Dynamic`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.2.3: `struct cafebabe::constant_pool::InvokeDynamic<'a>`

```rust
pub struct InvokeDynamic<'a> {
    pub attr_index: u16,
    pub name_and_type: cafebabe::constant_pool::NameAndType<'a>,
}
```

##### 7.2.3.1: Trait Implementations for `InvokeDynamic`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.2.4: `struct cafebabe::constant_pool::MemberRef<'a>`

```rust
pub struct MemberRef<'a> {
    pub class_name: Cow<'a, str>,
    pub name_and_type: cafebabe::constant_pool::NameAndType<'a>,
}
```

##### 7.2.4.1: Trait Implementations for `MemberRef`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.2.5: `struct cafebabe::constant_pool::MethodHandle<'a>`

```rust
pub struct MethodHandle<'a> {
    pub kind: cafebabe::constant_pool::ReferenceKind,
    pub class_name: Cow<'a, str>,
    pub member_kind: cafebabe::constant_pool::MemberKind,
    pub member_ref: cafebabe::constant_pool::NameAndType<'a>,
}
```

##### 7.2.5.1: Trait Implementations for `MethodHandle`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.2.6: `struct cafebabe::constant_pool::NameAndType<'a>`

```rust
pub struct NameAndType<'a> {
    pub name: Cow<'a, str>,
    pub descriptor: Cow<'a, str>,
}
```

##### 7.2.6.1: Trait Implementations for `NameAndType`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

### 7.3: Enums

#### 7.3.1: `enum cafebabe::constant_pool::BootstrapArgument<'a>`

```rust
pub enum BootstrapArgument<'a> {
    LiteralConstant(cafebabe::constant_pool::LiteralConstant<'a>),
    ClassInfo(Cow<'a, str>),
    MethodHandle(cafebabe::constant_pool::MethodHandle<'a>),
    MethodType(Cow<'a, str>),
}
```

##### 7.3.1.1: Trait Implementations for `BootstrapArgument`

**(Note: Does not implement common trait(s): `Clone`, `CloneToUninit`, `ToOwned`)**

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.2: `enum cafebabe::constant_pool::ConstantPoolItem<'a>`

```rust
pub enum ConstantPoolItem<'a> {
    LiteralConstant(cafebabe::constant_pool::LiteralConstant<'a>),
    ClassInfo(Cow<'a, str>),
    FieldRef(cafebabe::constant_pool::MemberRef<'a>),
    MethodRef(cafebabe::constant_pool::MemberRef<'a>),
    InterfaceMethodRef(cafebabe::constant_pool::MemberRef<'a>),
    NameAndType(cafebabe::constant_pool::NameAndType<'a>),
    MethodHandle(cafebabe::constant_pool::MethodHandle<'a>),
    MethodType(Cow<'a, str>),
    Dynamic(cafebabe::constant_pool::Dynamic<'a>),
    InvokeDynamic(cafebabe::constant_pool::InvokeDynamic<'a>),
    ModuleInfo(Cow<'a, str>),
    PackageInfo(Cow<'a, str>),
}
```

##### 7.3.2.1: Trait Implementations for `ConstantPoolItem`

**(Note: Does not implement common trait(s): `Clone`, `CloneToUninit`, `ToOwned`)**

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.3: `enum cafebabe::constant_pool::LiteralConstant<'a>`

```rust
pub enum LiteralConstant<'a> {
    Integer(i32),
    Float(f32),
    Long(i64),
    Double(f64),
    String(Cow<'a, str>),
    StringBytes(&'a [u8]),
}
```

##### 7.3.3.1: Trait Implementations for `LiteralConstant`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.4: `enum cafebabe::constant_pool::Loadable<'a>`

```rust
pub enum Loadable<'a> {
    LiteralConstant(cafebabe::constant_pool::LiteralConstant<'a>),
    ClassInfo(Cow<'a, str>),
    MethodHandle(cafebabe::constant_pool::MethodHandle<'a>),
    MethodType(Cow<'a, str>),
    Dynamic(cafebabe::constant_pool::Dynamic<'a>),
}
```

##### 7.3.4.1: Trait Implementations for `Loadable`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.5: `enum cafebabe::constant_pool::MemberKind`

```rust
pub enum MemberKind {
    Field,
    Method,
    InterfaceMethod,
}
```

##### 7.3.5.1: Trait Implementations for `MemberKind`

- `Copy`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.6: `enum cafebabe::constant_pool::ObjectArrayType<'a>`

```rust
pub enum ObjectArrayType<'a> {
    ArrayType(cafebabe::descriptors::FieldDescriptor<'a>),
    BinaryName(Cow<'a, str>),
}
```

##### 7.3.6.1: Trait Implementations for `ObjectArrayType`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

#### 7.3.7: `enum cafebabe::constant_pool::ReferenceKind`

```rust
pub enum ReferenceKind {
    GetField,
    GetStatic,
    PutField,
    PutStatic,
    InvokeVirtual,
    InvokeStatic,
    InvokeSpecial,
    NewInvokeSpecial,
    InvokeInterface,
}
```

##### 7.3.7.1: Trait Implementations for `ReferenceKind`

- `Copy`
- `Eq`
- `Hash`
- `PartialEq`
- `PartialOrd`
- `StructuralPartialEq`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

## 8: Module: `cafebabe::descriptors`

### 8.1: Common Traits

In addition to the crate's 'Common Traits', the following traits are commonly implemented by types in this module. Unless otherwise noted, you can assume these traits are implemented:

- `Clone`
- `Eq`
- `Hash`
- `PartialEq`
- `StructuralPartialEq`

- `RefUnwindSafe`
- `Send`
- `Sync`
- `UnwindSafe`

- `CloneToUninit` (`where T: Clone`)
- `ToOwned` (`where T: Clone`)
- `ToString` (`where T: Display + ?Sized`)

### 8.2: Structs

#### 8.2.1: `struct cafebabe::descriptors::ClassName<'a>`

```rust
pub struct ClassName<'a>();
```

Represents a valid binary class or interface name in the syntax of
the [JVM Spec](https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-4.html#jvms-4.2.1).

##### 8.2.1.1: Trait Implementations for `ClassName`

- `Display`
- `Ord`
- `PartialOrd`

- `convert::TryFrom<Cow<'a, str>>`

    ```rust
    impl<'a> convert::TryFrom<Cow<'a, str>> for cafebabe::descriptors::ClassName<'a> {
        type Error = cafebabe::error::ParseError;
    }
    ```
- `deref::Deref`

    ```rust
    impl<'a> deref::Deref for cafebabe::descriptors::ClassName<'a> {
        type Target = str;
    }
    ```

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)
- `deref::Receiver`

    ```rust
    where
        P: deref::Deref<Target = T> + ?Sized,
        T: ?Sized
    ```

#### 8.2.2: `struct cafebabe::descriptors::FieldDescriptor<'a>`

```rust
pub struct FieldDescriptor<'a> {
    pub dimensions: u8,
    pub field_type: cafebabe::descriptors::FieldType<'a>,
}
```

##### 8.2.2.1: Fields

###### 8.2.2.1.1: `dimensions`

Non-zero for array types denoting the arrays dimensions, otherwise `0`.

##### 8.2.2.2: Trait Implementations for `FieldDescriptor`

- `Display`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

#### 8.2.3: `struct cafebabe::descriptors::MethodDescriptor<'a>`

```rust
pub struct MethodDescriptor<'a> {
    pub parameters: Vec<cafebabe::descriptors::FieldDescriptor<'a>>,
    pub return_type: cafebabe::descriptors::ReturnDescriptor<'a>,
}
```

##### 8.2.3.1: Trait Implementations for `MethodDescriptor`

- `Display`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

### 8.3: Enums

#### 8.3.1: `enum cafebabe::descriptors::FieldType<'a>`

```rust
pub enum FieldType<'a> {
    Byte,
    Char,
    Double,
    Float,
    Integer,
    Long,
    Short,
    Boolean,
    Object(cafebabe::descriptors::ClassName<'a>),
}
```

##### 8.3.1.1: Trait Implementations for `FieldType`

- `Display`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

#### 8.3.2: `enum cafebabe::descriptors::ReturnDescriptor<'a>`

```rust
pub enum ReturnDescriptor<'a> {
    Return(cafebabe::descriptors::FieldDescriptor<'a>),
    Void,
}
```

##### 8.3.2.1: Trait Implementations for `ReturnDescriptor`

- `Display`

- `convert::TryFrom<U>` (`where U: convert::Into<T>`)

## 9: Module: `cafebabe::error`


### 9.1: Re-exports

- `struct cafebabe::error::ParseError` (See section 4.2.8: for details)


## 10: Module: `cafebabe::names`

